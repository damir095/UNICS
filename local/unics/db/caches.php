<?php
defined('MOODLE_INTERNAL') || die();

/**
 * Декларативные кеш-определения плагина (этап 3.3 аудита).
 *
 * identity - запросный (MODE_REQUEST): дедуплицирует identity-чтения
 * (запись ученика / роли по shortname / признак родителя), которые рельс,
 * навигация и гарды спрашивают по несколько раз за одну страницу.
 * Живёт в пределах HTTP-запроса - межзапросной инвалидации не требует.
 */
$definitions = [
    'identity' => [
        'mode'       => cache_store::MODE_REQUEST,
        'simplekeys' => true,
        'simpledata' => false,
    ],

    // Здоровье системы: результаты проверок. MODE_APPLICATION, потому что результат общий для
    // всех администраторов и должен переживать запрос - дешевые проверки считаются на каждой
    // странице ради полосы тревоги, а дорогие (сеть) вообще запускаются только по кнопке.
    // Срок жизни держим сами внутри значения: ядровый кеш TTL не предоставляет.
    'health' => [
        'mode'       => cache_store::MODE_APPLICATION,
        'simplekeys' => true,
        'simpledata' => false,
    ],
];
