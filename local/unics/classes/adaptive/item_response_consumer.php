<?php
namespace local_unics\adaptive;

defined('MOODLE_INTERNAL') || die();

/**
 * Маркер: оценщику нужны ответы по отдельным заданиям с параметрами IRT, а не только
 * итоговый процент за попытку.
 *
 * Нужен, чтобы ядро не знало имен реализаций. Сборка такого контекста
 * (`irt_attribution::element_responses_for_attempt`) стоит лишнего запроса на каждую
 * оцененную попытку, поэтому делать ее всегда - расточительно, а гадать по имени
 * компонента - возврат к жесткой связке, которую и убирали.
 *
 * Оценщик, реализующий этот маркер, получает в `$ctx['responses']` список
 * `{a, b, correct}` по элементу кодификатора. Не реализующий - не получает ничего
 * лишнего и работает по проценту за попытку.
 */
interface item_response_consumer {
}
